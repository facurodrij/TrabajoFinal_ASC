version: '3'
services:
  postgres:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 181020
      POSTGRES_DB: proyecto_software
    volumes:
      - ./data:/var/lib/postgresql/data
    ports:
      - "32768:5432"
